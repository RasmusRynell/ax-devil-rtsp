[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: marks tests that can run without hardware
    requires_hardware: Tests that require physical hardware access (cameras, etc)
